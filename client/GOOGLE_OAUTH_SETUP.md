# Google OAuth Setup Guide (Deprecated)

This project no longer uses Google OAuth or Supabase authentication.

Current authentication flow:

- Email and password login via backend endpoint: /api/auth/login
- Email and password signup via backend endpoint: /api/auth/signup
- JWT access token for protected API routes

This file is intentionally kept as a migration note so older setup steps are not reused.
